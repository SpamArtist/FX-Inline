# Backend API

NestJS backend for authentication, entitlements, billing, rates hardening, usage limits, and admin operations.

## Run

```bash
npm run backend:start
```

The server starts on `http://127.0.0.1:8787` by default.

## Key Endpoints

- `GET /rates/latest`

## Notes

- Data persists to `backend/data/db.json`.
