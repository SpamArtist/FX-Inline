{
  "name": "@fx-inline/control-plane-api",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "npm run build && node dist/server.js",
    "start": "node dist/server.js",
    "build": "tsc -p tsconfig.json",
    "test": "node --test",
    "db:migrate": "npm run build && node dist/db/migrate.js"
  }
}
