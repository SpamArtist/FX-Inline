# @fx-inline/settings-schema

Shared validation, sanitization, and manifest creation helpers for FX Inline inline-runtime settings.

The package is intentionally runtime-neutral. Browser extension code, backend export code, and future script runtimes should import these helpers instead of reimplementing schema defaults or scope normalization locally.
